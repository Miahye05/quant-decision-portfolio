# Data Notes

This folder is for external or generated project data.

- `external/`: optional local copies of user-provided inputs, ignored by git
- `processed/`: generated analysis-ready files, ignored by git

The recommended workflow is to keep external input files outside the repository and pass their folder path through `--data-dir`.
